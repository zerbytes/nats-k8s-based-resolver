{{- if .Values.rbac.enable }}
apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    {{- include "chart.labels" . | nindent 4 }}
  {{- if and .Values.manager.serviceAccount .Values.manager.serviceAccount.annotations }}
  annotations:
    {{- range $key, $value := .Values.manager.serviceAccount.annotations }}
    {{ $key }}: {{ $value }}
    {{- end }}
  {{- end }}
  name: {{ .Values.manager.serviceAccountName }}
  namespace: {{ .Release.Namespace }}
{{- end -}}
